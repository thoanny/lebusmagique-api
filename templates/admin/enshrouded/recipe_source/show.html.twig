{% extends 'admin.html.twig' %}

{% block title %}Source&nbsp;: {{ (recipe_source.item is defined) ? recipe_source.item.name : recipe_source.npc.name }}{% endblock %}

{% block body %}
    {% include '_breadcrumbs.html.twig' with {items: ['Enshrouded', 'Sources', 'Afficher']} %}
    <h1>Source&nbsp;: {{ (recipe_source.item is defined) ? recipe_source.item.name : recipe_source.npc.name }}</h1>

    <table class="table rounded-lg shadow w-full my-4">
        <tbody>
            <tr>
                <th width="1">Id</th>
                <td>{{ recipe_source.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ (recipe_source.item is defined) ? recipe_source.item.name : recipe_source.npc.name }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ (recipe_source.item is defined) ? 'Objet' : 'Personnage' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_enshrouded_recipe_source_index') }}" class="btn btn-outline">Retourner à la liste</a>

    {{ include('admin/enshrouded/recipe_source/_delete_form.html.twig') }}
{% endblock %}
