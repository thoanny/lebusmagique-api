{% if is_granted('ROLE_ADMIN') %}
<form method="post" action="{{ path('app_admin_enshrouded_recipe_category_delete', {'id': recipe_category.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ recipe_category.id) }}">
    <button class="btn btn-error mt-12">Supprimer la catégorie</button>
</form>
{% endif %}