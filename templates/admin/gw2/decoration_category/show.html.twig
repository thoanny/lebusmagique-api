{% extends 'admin.html.twig' %}

{% block title %}Catégorie de décorations{% endblock %}

{% block body %}
    <h1>Catégorie&nbsp;: {{ decoration_category.name }}</h1>

    <table class="table rounded-lg shadow w-full my-4">
        <tbody>
            <tr>
                <th width="1">#</th>
                <td>{{ decoration_category.id }}</td>
            </tr>
            <tr>
                <th>Catégorie</th>
                <td>{{ decoration_category.name }}</td>
            </tr>
        </tbody>
    </table>

    <div class="flex gap-4 justify-between mb-4">
        <a href="{{ path('app_admin_gw2_decoration_category_edit', {'id': decoration_category.id}) }}" class="btn btn-primary">Modifier</a>
        <a href="{{ path('app_admin_gw2_decoration_category_index') }}" class="btn btn-outline">Retourner à la liste</a>
    </div>

    {{ include('admin/gw2/decoration_category/_delete_form.html.twig') }}
{% endblock %}
