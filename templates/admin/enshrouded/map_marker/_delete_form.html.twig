{% if is_granted('ROLE_ADMIN') %}
<form method="post" action="{{ path('app_admin_enshrouded_map_marker_delete', {'id': map_marker.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ map_marker.id) }}">
    <button class="btn btn-error mt-12">Supprimer le marqueur</button>
</form>
{% endif %}