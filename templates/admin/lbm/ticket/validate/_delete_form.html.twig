<form method="post" action="{{ path('app_admin_lbm_ticket_validate_delete', {'id': validate.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ validate.id) }}">
    <button class="btn">Delete</button>
</form>
